<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width,initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { background:#f5f8f6; }
    .dash-sidebar { width:250px; background:linear-gradient(180deg,var(--dark-color),var(--primary-color)); min-height:100vh; }
    .dash-sidebar a { color:#e4f6e7; text-decoration:none; display:flex; align-items:center; gap:10px; padding:10px 14px; border-radius:8px; font-weight:500; }
    .dash-sidebar a.active, .dash-sidebar a:hover { background:rgba(255,255,255,.12); color:#fff; }
    .dash-header { background:#fff; border-bottom:1px solid #e2e8e4; }
    .metric-card { background:#fff; border:1px solid #e1eee4; border-radius:16px; padding:18px 18px 14px; position:relative; overflow:hidden; }
    .metric-card .icon { width:50px;height:50px;display:flex;align-items:center;justify-content:center;border-radius:14px; background:var(--gradient-primary);color:#fff; }
    .soft-orange { background:linear-gradient(135deg,#ffe1d5,#ffd2c0); }
    .soft-green { background:linear-gradient(135deg,#d7f5db,#c2ecd0); }
    .soft-accent { background:linear-gradient(135deg,#d9f0ff,#cde7fa); }
  </style>
</head>
<body>
<nav class="dash-header navbar navbar-light px-3">
  <div class="d-flex align-items-center gap-3">
    <a href="/" class="btn btn-sm btn-outline-success"><i class="fas fa-home"></i></a>
    <h1 class="h5 mb-0 fw-bold text-success">Dashboard</h1>
  </div>
  <div class="d-flex align-items-center gap-3">
    <div class="text-end small">
      <div class="fw-semibold">Admin</div>
      <div class="text-muted">Session</div>
    </div>
    <div class="rounded-circle bg-success d-flex align-items-center justify-content-center" style="width:40px;height:40px;color:#fff;">
      <i class="fas fa-user"></i>
    </div>
  </div>
</nav>
<div class="d-flex">
  <aside class="dash-sidebar p-3">
    <div class="mb-4 text-white small text-uppercase opacity-75">Navigation</div>
    <nav class="d-flex flex-column gap-1">
      <a href="/dashboard" class="<%= (section==='overview')? 'active':'' %>"><i class="fas fa-chart-line"></i> Vue d'ensemble</a>
      <a href="/dashboard/participants" class="<%= (section==='participants')? 'active':'' %>"><i class="fas fa-users"></i> Participants</a>
      <a href="/dashboard/badges" class="<%= (section==='badges')? 'active':'' %>"><i class="fas fa-id-badge"></i> Badges</a>
      <a href="/dashboard/emails" class="<%= (section==='emails')? 'active':'' %>"><i class="fas fa-envelope"></i> Emails</a>
  <a href="/dashboard/blogs" class="<%= (section==='blogs')? 'active':'' %>"><i class="fas fa-blog"></i> Blogs</a>
    </nav>
  </aside>
  <main class="flex-grow-1 p-4">
